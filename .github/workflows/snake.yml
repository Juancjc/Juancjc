# Este workflow foi consolidado em main.yml.
# Mantido aqui apenas como placeholder (nao roda automaticamente).
# Para remover de vez:
#   git rm .github/workflows/snake.yml
#   git commit -m "chore: remove workflow consolidado"

name: snake (deprecated)

on:
  workflow_dispatch:

jobs:
  noop:
    name: Deprecated - use main.yml
    runs-on: ubuntu-latest
    steps:
      - run: echo "Este workflow foi consolidado em .github/workflows/main.yml"
